package interfaces;

public interface PrintInfo {
    /**
     * For the Task 1 in chapter OOP paradigms, move method printUserInfo()
     * to interface PrintInfo. For this interface method must have name printInfo().
     * Both classes User and Manager must implement this interface.
     */
    void printInfo();
}
